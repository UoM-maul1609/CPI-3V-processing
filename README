CPI-3V processing
=================

The current setup and usage documentation is in README.md.

Quick setup from the repository root:

    python -m venv .venv
    source .venv/bin/activate
    python -m pip install -e ".[gui]"
    cpi-calibrate

Deactivate the environment with:

    deactivate
